{
  "name": "@preliquify/preact",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "publish": "tsc -p tsconfig.json && npm version patch && npm publish"
  },
  "peerDependencies": {
    "preact": "^10.22.0",
    "@preliquify/core": "workspace:*"
  },
  "dependencies": {
    "@preliquify/core": "workspace:*"
  }
}
